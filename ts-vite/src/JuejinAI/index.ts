// // 使用 import.meta.glob 引入所有非测试的 .ts 文件，排除 .test.ts 文件
// const modules = import.meta.glob('./**/!(*.test).ts');

// // 动态导入所有模块
// Object.values(modules).forEach(module => module());
import './01.找单独的数';
import './02.徒步旅行中的补给问题';
import './03.数字字符串格式化';